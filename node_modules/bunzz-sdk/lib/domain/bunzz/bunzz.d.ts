import { Handler } from '../handler/handler';
import { MetaMaskUpdateTypes } from '../../utils/metamask';
interface BunzzConfig {
    apiKey: string;
    dappId: string;
    onChangingChain?: MetaMaskUpdateTypes;
    onChangingSigner?: MetaMaskUpdateTypes;
}
export declare const bunzz: {
    initializeHandler: (params: BunzzConfig) => Promise<Handler>;
};
export {};
