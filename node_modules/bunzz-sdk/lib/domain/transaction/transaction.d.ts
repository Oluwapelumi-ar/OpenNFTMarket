import { ethers } from 'ethers';
import { TransactionResponse as EthersTransaction } from '@ethersproject/abstract-provider';
declare type TransactionReceipt = {
    blockNumber: number;
    blockHash: string;
    transactionHash: string;
    success: boolean;
    events: Event[];
    rawReceipt: ethers.providers.TransactionReceipt;
};
declare type Event = {
    name: string;
    signature: string;
    args: string[];
};
export default class Transaction {
    readonly hash: string;
    readonly rawTransaction: EthersTransaction;
    constructor(hash: string, tx: EthersTransaction);
    wait(): Promise<TransactionReceipt>;
}
export {};
