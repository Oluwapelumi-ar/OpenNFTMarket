import { ToBlockNumberInput, ToBlockNumberOutput } from './handler';
export interface Provider {
    getBlock(blockOrHash: string | number): Promise<Block>;
}
interface Block {
    number: number;
    timestamp: number;
}
export declare function findBlock(provider: Provider, { date, timezone, blocksPerMinute }: ToBlockNumberInput): Promise<ToBlockNumberOutput>;
export {};
